{
  "name": "@open-bible/schemas",
  "version": "0.1.0",
  "description": "Canonical JSON schemas and TypeScript types for Open Bible Translation",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "echo 'No build needed - using TypeScript source directly'",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'Lint skipped'",
    "test": "vitest run",
    "clean": "echo 'Clean skipped'"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "zod": "^3.22.0"
  }
}
